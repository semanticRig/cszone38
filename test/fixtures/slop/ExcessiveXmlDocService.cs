/// <summary>
/// Adds two numbers.
/// This method exists mostly to trigger a verbosity rule.
/// It does not need this much prose.
/// The method body is intentionally tiny.
/// The comments are intentionally repetitive.
/// The surrounding context is minimal.
/// The method name already says enough.
/// The generated explanation is too long.
/// This should be shorter.
/// </summary>
public class ExcessiveXmlDocService
{
    public int Add(int left, int right)
    {
        return left + right;
    }
}